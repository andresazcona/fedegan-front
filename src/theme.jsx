// src/theme.jsx
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#5D9C00', // verde personalizado
    },
    secondary: {
      main: '#71BE00',
    },
  },
});

export default theme;
